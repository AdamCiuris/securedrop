events {}

http {
    server {
        listen 80;
        server_name _;

        root /usr/share/nginx/html;

        # Serve index.final.html as default
        index index.final.html;

        location / {
            try_files $uri $uri/ =404;
        }
    }
}